---
// src/pages/alt.astro — redirects to homepage now that the alt design is live
import { getEntry } from 'astro:content';
const base = import.meta.env.BASE_URL;
return Astro.redirect(`${base}/`);
---
