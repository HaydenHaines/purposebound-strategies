---
// src/components/SectionLabel.astro
interface Props {
  text: string;
  light?: boolean;
}
const { text, light = false } = Astro.props;
---
<p class={`${light ? 'text-accent' : 'text-link'} font-inter text-[10px] uppercase tracking-[0.25em] font-bold mb-3`}>
  {text}
</p>
